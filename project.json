{
  "name": "root",
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "./",
  "targets": {
    "build": {
      "command": "echo \"  LocoSync > All packages built\"",
      "dependsOn": ["^build:types", "^build"]
    }
  },
  "implicitDependencies": [
    "@loco-sync/client",
    "@loco-sync/react",
    "@loco-sync/idb"
  ]
}
